<% const links = typeof links !== "undefined" ? links : [] %>

<nav>
  <ul>
    <% links.forEach(link => { %>
      <li><a href="<%= link.href %>"><%= link.text %></a></li>
    <% }) %>
  </ul>
</nav>
